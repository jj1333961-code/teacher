# Student System AI — نسخة التطوير

هذه النسخة تعدّل المشروع الحالي نفسه ولا تنشئ مشروعاً جديداً.

## Gemini وGroq
- `GEMINI_API_KEY` و`GROQ_API_KEY` يبقيان Server-side فقط.
- Gemini (`gemini-2.5-flash`) هو المزوّد الأساسي للنص والصوت.
- عند تعذر Gemini ينتقل النص إلى Groq (`llama-3.3-70b-versatile`).
- للصوت، يفرّغ Groq Whisper (`whisper-large-v3-turbo`) التسجيل ثم يعالجه نموذج Groq النصي.
- لا يقبل البديل النصي هوية متحدث ولا ينشئ بصمة صوتية مزعومة؛ التحقق البيومتري الكامل يحتاج Gemini أو خدمة متخصصة.

## ما تم تحسينه
- حساب درجة الأسئلة الصوتية و«أكمل» حسب عدد الآيات الناقصة.
- منع حفظ/إرسال تسجيل التسميع عندما تفشل مطابقة المقرر.
- الحفاظ على منع حفظ تسجيل الصوت عندما تفشل مطابقة بصمة الطالب.
- إضافة خريطة آيات فعلية للـ30 جزءاً، بما فيها الأجزاء التي تبدأ/تنتهي في منتصف السورة.
- عرض حدود الآيات الحقيقية للجزء عند اختيار جزء الطالب.
- انتقال تلقائي وآمن من Gemini إلى Groq للنص والتفريغ الصوتي.

## النشر على Vercel
بعد رفع الملفات إلى المستودع المرتبط بالمشروع، اضبط `GEMINI_API_KEY` و`GROQ_API_KEY` في متغيرات بيئة الخادم ثم أعد Deploy.

## مساعد تطوير الموقع — التطبيق التلقائي
لتفعيل التطبيق التلقائي من مساعد المسؤول، أضف متغيرات البيئة التالية في Vercel/الخادم:
- `DEV_ASSISTANT_AUTO_APPLY=true`
- `GITHUB_TOKEN` (Fine-grained token بصلاحية Contents: Read and write للمستودع فقط)
- `GITHUB_OWNER`
- `GITHUB_REPO`
- `GITHUB_BRANCH` (مثلاً `main`)
- `GITHUB_PUBLISHER_EMAILS` (عناوين Google الموثقة المسموح لها بالنشر، مفصولة بفواصل)

سجّل الدخول من نفس النطاق عبر Google OAuth. صلاحية GitHub المطلوبة هي Contents: Read and write للمستودع المحدد فقط؛ لا تعتمد حماية الخادم على `role` أو `adminId` المرسل من المتصفح.
جميع هذه القيم Server-side فقط. لا تضعها في `public/index.html` أو أي JavaScript يصل إلى المتصفح.
المساعد يقرأ الملفات التي تحددها الخطة، يولّد المحتوى الكامل للملفات المعدلة، ثم يحفظها عبر GitHub Contents API. لا يسمح بحذف الملفات أو تعديل `.env`، والحد التلقائي 8 ملفات و500KB لكل ملف.
